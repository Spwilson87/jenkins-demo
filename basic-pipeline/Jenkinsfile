pipeline {
    agent any
    stages {
        stage('Pipeline Stages'){
            steps {
                sh "ls"
                sh "pwd"
                sh "mkdir pipeline-dir"
                sh "cd pipeline-dir"
                sh "ls"
            }
        }
    }
}
